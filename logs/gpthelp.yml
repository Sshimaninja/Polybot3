Alright, now I'm getting a 'replacement fee too low', however, I don't want the transaction re-submitted. It's trying to send the same tx with different params over and over, even though I've tried to use a variable to make sure it doesn't try to send the same tx multiple times:

/mnt/d/code/arbitrage/polybot-live/polybotv3/node_modules/@ethersproject/logger/src.ts/index.ts:269
        const error: any = new Error(message);
                           ^
Error: replacement fee too low [ See: https://links.ethers.org/v5-errors-REPLACEMENT_UNDERPRICED ] (error={"reason":"processing response error","code":"SERVER_ERROR","body":"{\"jsonrpc\":\"2.0\",\"id\":170,\"error\":{\"code\":-32000,\"message\":\"replacement transaction underpriced\"}}","error":{"code":-32000},"requestBody":"{\"method\":\"eth_sendRawTransaction\",\"params\":[\"

The var is tradePending, and I have it defined in the code as:
```
const warning = 0
const tradePending = false;
const slippageTolerance = BN(0.01)
var pendingID: string | undefined

export async function control(data: FactoryPair[], gasData: any) {
	const promises: any[] = [];

	for (const pair of data) {
		console.log("ExchangeA: " + pair.exchangeA + " ExchangeB: " + pair.exchangeB + " matches: " + pair.matches.length, " gasData: " + gasData.fast.maxFee + " " + gasData.fast.maxPriorityFee);

		for (const match of pair.matches) {
			if (!tradePending && match.poolA_id !== pendingID && match.poolB_id !== pendingID) {

				const r = new Reserves(match);
				const reserves = await r.getReserves(match);

				const p0 = new Prices(match.token0, match.token1, match.poolA_id, reserves[0]);
				const p1 = new Prices(match.token0, match.token1, match.poolB_id, reserves[1]);

				const t = new Trade(pair, match, p0, p1, slippageTolerance, gasData);
				const trade = await t.getTrade();

				const dataPromise = tradeLogs(trade);
				const rollPromise = rollDamage(trade, await dataPromise, warning, tradePending, pendingID);

				promises.push(dataPromise, rollPromise);
			}
		}
	}

	await Promise.all(promises).catch((error: any) => {
		console.log("Error in control.ts: " + error.message);
		return;
	});
}

```

Then in the rollDamage function, I have:
```typescript

export async function rollDamage(trade: BoolTrade, data: any, warning: number, tradePending: boolean, pendingID: string | undefined) {

	// Conversion to BN because BN works with decimals
	const profpercBN = BN(u.formatUnits(trade.profitPercent, trade.tokenOut.decimals))

	if (profpercBN.gt(BN(0)) && trade.loanPool.reserveInBN.gt(BN(1)) && trade.loanPool.reserveOutBN.gt(BN(1)) && trade.recipient.reserveInBN.gt(BN(1)) && trade.recipient.reserveOutBN.gt(BN(1)) /* || profpercBN.lt(0)*/) { // May need to take fees into account here, but testing now.

		logger.info(await data)

		const actualProfit = await gasVprofit(trade)

		if (BN(actualProfit.profit).gt(0) && warning === 0) {
			logger.info("Profitable trade found on " + trade.ticker + "!")
			logger.info("Profit: ", actualProfit.profit.toString(), "Gas Cost: ", u.formatUnits(actualProfit.gas.gasPrice, 18), "Flash Type: ", trade.type)
			tradePending = true
			pendingID = trade.recipient.pool.address

			await execute(trade, actualProfit)

			warning++
			return warning
		}

		if (BN(actualProfit.profit).gt(0) && warning === 1) {
			logger.info("Trade pending on " + pendingID + "?: ", tradePending)
			warning++
			return warning
		}

		if (BN(actualProfit.profit).gt(0) && warning > 1) {
			return
		}

		if (BN(actualProfit.profit).lte(0)) {
			console.log("<<<<<<<<<<<<No Trade: " + trade.ticker + " [ gas > profit ] >>>>>>>>>>>>")
			console.log(data)
			return
		}

		if (actualProfit.profit == undefined) {
			console.log("Profit is undefined: error in gasVProfit")
			return
		}

	} else if (profpercBN.lt(0) /*&& profpercBN.gt(-0.6)*/) { // TESTING
		console.log("<<<<<<<<<<<<No Trade: " + trade.ticker + " [ profit < 0.3% | " + profpercBN.toFixed(trade.tokenOut.decimals) + " ] >>>>>>>>>>>>")
		// console.log(await data)
		// console.log(data.basicData)
		return
	}
}
```
Then in the execute function, I have:
```typescript
export async function execute(
	trade: BoolTrade,
	profit: Profit,
): Promise<TxData> {
	console.log('::::::::::::::::::::::::::::::::::::::::BEGIN TRANSACTION: ' + trade.ticker + '::::::::::::::::::::::::::')

	var gasbalance = await checkGasBal();

	console.log("Wallet Balance Matic: " + ethers.utils.formatUnits(gasbalance, 18) + " " + "MATIC")

	let result: TxData = {
		txResponse: undefined,
		tradePending: false,
	}

	if (trade) {
		console.log("Wallet Balance Matic: " + ethers.utils.formatUnits(gasbalance, 18) + " " + "MATIC")
		console.log("Gas Cost::::::::::::: " + ethers.utils.formatUnits(profit.gas.gasPrice, 18) + " " + "MATIC")

		const gotGas = profit.gasCost.lt(gasbalance)

		gotGas == true ? console.log("Sufficient Matic Balance. Proceeding...") : console.log(">>>>Insufficient Matic Balance<<<<")

		if (gotGas == false) {
			console.log(":::::::::::::::::::::::END TRANSACTION: " + trade.ticker + ': GAS GREATER THAN PROFIT::::::::::::::::::::::::: ')
			return result;
		}

		if (gotGas == true) {

			let gasEstimate = await fetchGasPrice(trade);
			let gasObj: TxGas = {
				type: 2,
				// gasPrice: gasLimit,
				maxFeePerGas: Number(profit.gas.maxFee),
				maxPriorityFeePerGas: Number(profit.gas.maxPriorityFee),
				gasLimit: gasEstimate.gasEstimate,
				// nonce: nonce,
			}
			//Notify of possible trade (after testing move to post-profitable trade)
			// await notify(trade, profit);

			console.log(":::::::::::Sending Transaction::::::::::: ")

			result.tradePending = true;

			await notify(trade, profit);

			const req = await send(trade, gasObj);

			const logs = await logEmits(trade, req);

			logger.info("Transaction logs: \n" + logs)

			//Print balances after trade
			await checkBal(trade.tokenIn.id, trade.tokenIn.decimals, trade.tokenOut.id, trade.tokenOut.decimals)

			result = {
				txResponse: req.txResponse,
				tradePending: false,
			}

			console.log("::::::::::::::::::::::::::::::::::::::::END TRANSACTION::::::::::::::::::::::::::::::::::::::::")

			return result;

		} else {

			console.log("::::::::::::::::::::::::::::::::::::::::TRADE UNDEFINED::::::::::::::::::::::::::::::::::::::: ")

			console.log("::::::::::::::::::::::::::::::::::::::::END TRANSACTION::::::::::::::::::::::::::::::::::::::::")

			return result;
		}
	} else {
		console.log("::::::::::::::::::::::::::::::::::::::::END TRANSACTION::::::::::::::::::::::::::::::::::::::::")
		return result;
	}
}
And it's also passed to the send function:
```typescript

export async function send(trade: BoolTrade, gasObj: TxGas): Promise<TxData> {

	let tx: V2Tx = await trade.flash.flashSwap(
		trade.loanPool.factory.address,
		trade.loanPool.router.address,
		trade.recipient.router.address,
		trade.tokenIn.id,
		trade.tokenOut.id,
		trade.recipient.tradeSize,
		trade.recipient.amountOut,
		trade.loanPool.amountRepay,
		gasObj
	);

	async function send(tx: any): Promise<TxData> {
		let signedTx = await wallet.signTransaction(tx);
		let txResponse = await provider.sendTransaction(signedTx);
		await txResponse.wait(10);
		console.log("Tx sent. txHash: " + txResponse.hash + " Awaiting confirmation. Confirmations: " + txResponse.confirmations)
		if (txResponse.blockHash != null) {
			console.log("Transaction confirming. Block Hash: " + txResponse.blockHash)
			//Wait until tx is mined to return tradePending == false.
			await txResponse.wait(30);
			return {
				txResponse: txResponse,
				tradePending: false
			}
		} else {
			console.log("Transaction failed. Error: " + txResponse)
			return {
				txResponse: txResponse,
				tradePending: false
			}
		}
	}

	try {

		const t = await send(tx)
		return t

	} catch (error: any) {

		if (error.includes("transaction underpriced")) {
			console.log("Error TRANSACTION UNDERPRICED in execute.ts " + error.message)

			gasObj = {
				type: 2,
				maxFeePerGas: gasObj.maxFeePerGas + 10,
				maxPriorityFeePerGas: gasObj.maxPriorityFeePerGas + 10,
				gasLimit: gasObj.gasLimit.add(BigNumber.from(10000)),
			}

			const newTx = await send(tx)

			console.log("Retrying transaction with new gas price: " + gasObj.maxFeePerGas)

			// Wait for the new transaction to be confirmed
			await newTx.txResponse.wait(1);

			return {
				txResponse: newTx.txResponse,
				tradePending: false
			}
		} else {
			console.log("Transaction failed. Error: " + error.message)
			return {
				txResponse: "error",
				tradePending: false
			}
		}
	}
}
```

I'm thkning this is not working as I intend it.

Is there a better way to integrate this so that any trades on the same pool are skipped until the first one is confirmed?